# Wrapping/Customizing a shadcn/ui Component

`src/components/ui/` is generated by the shadcn CLI and is **read-only** — never hand-edit it.

## Files
- `src/components/shared/<Thing>.tsx` — the wrapper, sole consumer of `@/components/ui/<thing>` primitives
- Every call site across the app — update to import the wrapper instead of the raw `ui/` component

## Constraints
- One wrapper per `ui/` component, drive all project-specific behavior (RTL positioning, brand colors, fixed
  sizing, a baked-in variant) through typed props — not free-form `className` overrides at each call site.
- Grep to confirm nothing outside the wrapper still imports the raw primitive:
  `grep -rln "from '@/components/ui/<thing>'" src --include="*.tsx" | grep -v "<Thing>.tsx"` should return nothing.
- If the vendor file was hand-edited at some point, reset it with `npx shadcn add <name> --overwrite --yes`, then
  re-verify the wrapper still produces the desired look — the fix belongs entirely in the wrapper.
- Run `npm run typecheck && npm run lint:check && npm test` before considering it done.
- **Full worked examples, known gotchas (twMerge custom-token dedup, `dark:` variant firing under OS dark mode,
  Radix `Select` + RTL positioning, RTL `items-end`/`items-start` inversion) live in `../GOTCHAS.md` — read it before
  starting a wrapper, it documents real bugs hit doing exactly this.**

## References
- `src/components/shared/AppDialog.tsx`, `Button.tsx`, `LtrInput.tsx`, `Toggle.tsx` — see `../GOTCHAS.md` for what
  each one fixes and why
